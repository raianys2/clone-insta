export default function ItemMenu (props) {
    return(
        <>
        <li>
        <span>{props.icones}</span>
        <span>{props.Home}</span>
        <span>{props.Eplorar}</span>
    </li>

        </>
    )
}